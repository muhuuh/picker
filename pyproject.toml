[project]
name = "stock-research"
version = "0.1.0"
description = "Deterministic stock tracking and research repo tooling."
requires-python = ">=3.10"
dependencies = [
    "openai-agents>=0.15.2,<0.16",
    "yfinance>=1.2.2",
]

[tool.pytest.ini_options]
pythonpath = ["."]

[tool.setuptools.packages.find]
include = ["stock_research*"]
