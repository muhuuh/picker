[project]
name = "stock-research"
version = "0.1.0"
description = "Deterministic stock tracking and research repo tooling."
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["."]
